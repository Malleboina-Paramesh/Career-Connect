"use client";
import Editor from "@/components/Editor/Editor";

import React from "react";

const page = () => {
  return <Editor content={undefined} setContent={() => {}} />;
};

export default page;
